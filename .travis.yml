language: node_js
node_js:
  - "4"
  - "6"
before_install:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - npm install -g add-free-course-udemy
script:
  - echo test